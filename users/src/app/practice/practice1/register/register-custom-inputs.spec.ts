import { RegisterCustomInputs } from './register-custom-inputs';

describe('RegisterCustomInputs', () => {
  it('should create an instance', () => {
    expect(new RegisterCustomInputs()).toBeTruthy();
  });
});
