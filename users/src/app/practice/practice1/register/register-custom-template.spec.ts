import { RegisterCustomTemplate } from './register-custom-template';

describe('RegisterCustomTemplate', () => {
  it('should create an instance', () => {
    expect(new RegisterCustomTemplate()).toBeTruthy();
  });
});
